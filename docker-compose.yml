version: '3.8'

services:
  web:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:-changeme}
    volumes:
      - sessions-data:/app/sessions.json
    restart: unless-stopped

volumes:
  sessions-data:
